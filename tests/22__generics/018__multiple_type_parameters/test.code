type Pair<K, V> = struct {
    key: K
    value: V
}

proc main() -> i32 {
    let pair = Pair<str, i32>(key: "answer", value: 42)
    let swapped = Pair<i32, str>(key: pair.value, value: pair.key)
    return swapped.key - 42
}
