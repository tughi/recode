type Foo = struct {
    value: i32
}

proc identity<T>(value: T) -> T {
    return value
}

proc wrap<T>(value: T) -> T {
    return value
}

proc main() -> i32 {
    let foo = wrap(Foo(value: 42))
    return identity(foo.value) - 42
}
