type Dog = struct {
    age: i32
}

proc (^Dog).legs(self) -> i32 {
    return 4
}

type Animal = trait {
    proc legs(self) -> i32
}

proc main() -> i32 {
    let dog = Dog(age: 10)
    let animal = Animal(^dog)
    return 0
}
