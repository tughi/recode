type Point = struct {
    x: i32
    y: i32
}

type Path = struct {
    points: [4]Point
}

proc main() -> i32 {
    let path: Path
    path.points[0] = Point(x: 42, y: 7)
    return path.points[0].y - 7
}
