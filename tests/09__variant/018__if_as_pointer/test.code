type Value = variant {
    bool
    i32
}

proc main() -> i32 {
    let value: Value = 42
    let pointer = ^value

    if pointer is ^i32 as integer {
        return integer.^ - 42
    }

    return 1
}
