type Value = variant {
    bool
    i32
}

proc reset(number: ^i32) {
    number.^ = 0
}

proc main() -> i32 {
    let value: Value = 42

    if value is i32 as number {
        reset(number: ^number)
    }

    if value is i32 as number {
        return number
    }

    return 1
}
