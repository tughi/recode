type Value = variant {
    bool
    i32
}

proc main() -> i32 {
    let value: Value = 42

    if (value is i32 as number and number > 0) or value is bool {
        return number
    }

    return 1
}
