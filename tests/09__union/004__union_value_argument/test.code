package test

union Value {
    bool
    i32
}

proc main() -> i32 {
    let value: Value
    value = 42
    let other_value: Value
    other_value = value
    let another_value = demo(value: true)
    return 0
}

proc demo(value: Value) -> Value {
    return value
}
