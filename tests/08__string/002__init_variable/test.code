package test

proc main() -> i32 {
    let empty: str = ""
    let quote: str = "Answer to the Ultimate Question of Life, the Universe, and Everything"
    return 0
}

type String = struct {
    data: [^]u8
    length: usize
}
