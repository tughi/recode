import compiler.source

type Module = struct {
    source: source.Source
    next_module: ^Module
}

type Package = struct {
    name: str
    symbols: ^Symbols
    first_module: ^Module
    next_package: ^Package
}
