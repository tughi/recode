import os
import sdl
import gui
import gui.app
import gui.button

proc main() -> i32 {
    try app.init() else {
        os.stderr.write_string("Failed to initialize app: ").write_string(error.message).end_line()
        return 1
    }

    let window = try app.create_window(title: "Editor", width: 800, height: 600) else {
        os.stderr.write_string("Failed to create window: ").write_string(error.message).end_line()
        return 1
    }

    let button: gui.Widget = gui.Button(
        bounds: gui.Rect(left: 50, top: 50, right: 200, bottom: 100)
        label: "Click Me"
    )
    window.widget = ^button

    return app.run()
}
