type Widget = trait {
    proc layout(self, view: ^View)
    proc render(self, view: ^View, renderer: ^Renderer)
}

type Widget_Factory = trait {
    proc create(self) -> Widget !> allocator.Out_Of_Memory
}
