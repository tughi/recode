\ Local variables

putchar :: (char: Int) -> Nothing

main :: () -> Int {
    message: @Int8
    message = "Hello world!\n"
    {
        index: Int
        index = 0
        loop {
            if (index >= 13) {
                result: Int
                result = 0
                return result
            }
            putchar(message[index] as Int)
            index = index + 1
        }
    }
}
