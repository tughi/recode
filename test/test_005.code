\ Test mixing int and boolean expressions

test: Boolean
noise: Int

main :: () -> Int {
    test = (1 > 0)
    noise = 256

    if (true == false) {
        return 2
    }

    if (test == true) {
        return 0
    }

    return 1
}
